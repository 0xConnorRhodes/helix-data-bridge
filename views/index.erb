<h1>Helix Data Bridge</h1>

<h2>Status</h2>
<h3>api key</h3>
<% if $api_key_status %>
	<%= "API key configured for org: #{$org_id}" %>
<% else %>
	<%= "No API key configured. Please configure a key using the link below." %>
<% end %>

<h3>config check</h3>
<% if $config_message.any? %>
	<%= $config_message.join("\n") %>
<% else %>
	<%= "Configuration will be checked after API key is configured and both configuration files are uploaded. Please upload configuration files after adding your API key." %>
<% end %>

<h3>config files added</h3>
<ul>
<% if File.exist?('event_types_config.csv') %>
	<li>Event types config file added.</li>
<% else %>
	<li>Event types config file has not been added.</li>
<% end %>

<% if File.exist?('devices_config.csv') %>
	<li>Device mappings config file added.</li>
<% else %>
	<li>Device mappings config file has not been added.</li>
<% end %>
</ul>

<h2>Configuration</h2>
<p>
	<ul>
		<li><a href="/config/api-key">Configure API key</a></li>
		<li><a href="/config/event-types">Configure event translation</a>
		<li><a href="/config/device-mappings">Configure device mappings</a></li>
	</ul>
</p>