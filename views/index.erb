<h1>Verkada Helix Data Translator</h1>

<h2>Status</h2>
<h3>api key</h3>
<% if $api_key_status %>
	<%= "API key configured for org: #{$org_id}" %>
<% else %>
	<%= "No API key configured. Please configure a key using the link below." %>
<% end %>

<h3>config check</h3>
<% if $api_key_status %>
	<% if $config_message.any? %>
	    <%= $config_message.join("\n") %>
	<% else %>
	    <%= "Configuration checks passed. Ready to translate events." %>
	<% end %>
<% else %>
	<%= "Configuration will be checked after API key is configured." %>
<% end %>

<h2>Configuration</h2>
<p>
	<ul>
		<li><a href="/config/api-key">Configure API key</a></li>
		<li><a href="/config/event-types">Configure event translation</a>
		<li><a href="/config/devices">Configure device mappings</a></li>
	</ul>
</p>